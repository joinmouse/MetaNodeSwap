{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.26.5",
    "dotenv": "^16.3.1"
  },
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy ignition/modules/UniswapV2.js --network sepolia",
    "verify": "hardhat ignition verify chain-11155111 --include-unrelated-contracts",
    "check": "npx hardhat run scripts/verify-deployment.js --network sepolia"
  }
}
